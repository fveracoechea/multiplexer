{
  "name": "multiplexer",
  "version": "0.0.0",
  "description": "A tmux-based orchestration tool/MCP server for coding agents.",
  "type": "module",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "biome lint",
    "format": "biome format --write",
    "fix": "biome check --write",
    "check": "bun run typecheck && biome check && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.5",
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  }
}
